# Copy to .env and adjust for local development if the defaults don't fit.
# All variables are optional; sensible defaults are baked into app/core/config.py.

APP_ENVIRONMENT=development
APP_CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
